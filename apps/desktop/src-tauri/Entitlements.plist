<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!-- Applied at codesign time (bundle.macOS.entitlements). The release build
     signs with the hardened runtime, which blocks microphone capture unless
     this entitlement is granted — without it, audio memos fail silently in
     notarized builds. -->
<plist version="1.0">
<dict>
	<key>com.apple.security.device.audio-input</key>
	<true/>
	<!-- Read-only Apple Calendar access for the daily-note events panel
	     (src/calendar.rs); without it the hardened runtime blocks EventKit
	     in notarized builds. -->
	<key>com.apple.security.personal-information.calendars</key>
	<true/>
	<!-- Apple Contacts integration: read-only CNContactStore lookups for
	     person notes. Without this the hardened runtime blocks the contacts
	     permission prompt in notarized builds. -->
	<key>com.apple.security.personal-information.addressbook</key>
	<true/>
	<!-- iCloud Drive document sync (Plan 21). These are *restricted*
	     entitlements: codesign only honors them when a provisioning profile
	     granting them is embedded in the app — that's DayJot.provisionprofile
	     (stable) / DayJot-beta.provisionprofile, placed at
	     Contents/embedded.provisionprofile by `bundle.macOS.files` before
	     signing. The profiles are bound to the Developer ID Application
	     certificate expiring 2027-02-01 (portal id AZZYZ7YTGR); a certificate
	     rotation or App ID capability edit means regenerating and
	     re-committing both. The container id must stay byte-identical to the
	     iOS app's (gen/apple entitlements) — it IS the shared folder both
	     apps sync through, and container ids are permanent. -->
	<key>com.apple.developer.icloud-container-identifiers</key>
	<array>
		<string>iCloud.app.dayjot</string>
	</array>
	<key>com.apple.developer.icloud-services</key>
	<array>
		<string>CloudDocuments</string>
	</array>
	<key>com.apple.developer.ubiquity-container-identifiers</key>
	<array>
		<string>iCloud.app.dayjot</string>
	</array>
</dict>
</plist>
