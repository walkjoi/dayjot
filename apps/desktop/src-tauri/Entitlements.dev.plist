<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!-- The dev flavor's entitlements: Entitlements.plist minus the iCloud block.
     iCloud entitlements are *restricted* — claiming them requires a matching
     provisioning profile, and only the stable/beta App IDs have one. A signed
     dev-flavor build claiming them would be killed at launch. Keep in sync
     with Entitlements.plist for everything else. -->
<plist version="1.0">
<dict>
	<key>com.apple.security.personal-information.calendars</key>
	<true/>
	<key>com.apple.security.personal-information.addressbook</key>
	<true/>
</dict>
</plist>
