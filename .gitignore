# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment secrets (never commit; use OS keychain or CI secrets)
.env
.env.*
!.env.example

# Monorepo / build caches
.turbo
/target
*.tsbuildinfo

# DayJot graph index + local state (rebuildable projection; never committed)
.dayjot/

# Claude Code session artifacts (shared hooks in settings.json stay tracked)
.claude/*
!.claude/settings.json
